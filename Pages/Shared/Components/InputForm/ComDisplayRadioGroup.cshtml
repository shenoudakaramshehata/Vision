@using Vision.Models;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model AdTemplateConfig

@*<style>
    .profile-edit-container .custom-form label i {
        top: 0 !important;
    }
</style>
*@@*<label>@Model.AdTemplateFieldCaptionEn <i style="padding-left: 3rem !important;" class="fal fa-briefcase"></i></label>
*@


<div class="dashboard-title  dt-inbox fl-wrap">
    <h3>@Model.AdTemplateFieldCaptionEn</h3>
</div>


<div class="profile-edit-container fl-wrap block_box">
    <div class=" col-md-6">

        @{
            foreach (var item in Model.AdTemplateOptions)
            {
                <div class="form-check col-md-2">
                    <label class="form-check-label" for="@item.AdTemplateOptionId">
                        @item.OptionEn
                    </label>
                    <input class="form-check-input" type="radio" name="@item.AdTemplateConfigId" id="@item.AdTemplateOptionId" value="@item.AdTemplateOptionId">

                </div>
            }
        }

    </div>
</div>

@*<ul class="fl-wrap filter-tags no-list-style ds-tg">
    @{
        foreach (var item in Model.AdTemplateOptions)
        {
            <li>
                <input type="radio" name="@item.AdTemplateConfigId" id="@item.AdTemplateOptionId" value="@item.AdTemplateOptionId">
                <label for="@item.AdTemplateOptionId">@item.OptionEn</label>
            </li>
        }
    }
</ul>*@















