@inject CRMDBContext _context
@using Vision.Models;
@using Vision.Data;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model AdContent
<!--box-widget-item -->
@*<div class="box-widget-item fl-wrap block_box">
    <div class="box-widget-item-header">
    <h3>@Model.AdTemplateConfig.AdTemplateFieldCaptionEn</h3>
    </div>
    <div class="box-widget fl-wrap">
    <div class="box-widget-content">
    <div class="list-single-tags tags-stylwrap">
    @{
    foreach (var item in @Model.AdContentValues)
    {
    var CheckBoxValue = _context.AdTemplateOptions.Where(e => e.AdTemplateOptionId == Convert.ToInt32(@item.ContentValue)).FirstOrDefault().OptionEn;

    <a> @CheckBoxValue</a>
    }
    }


    </div>
    </div>
    </div>
    </div>*@
<!--box-widget-item end -->
<!-- list-single-main-item -->
<div class="list-single-main-item fl-wrap block_box">
    <div class="list-single-main-item-title">
        <h3>@Model.AdTemplateConfig.AdTemplateFieldCaptionEn</h3>
    </div>
    <div class="list-single-main-item_content fl-wrap">
        <div class="listing-features fl-wrap">
            <ul class="no-list-style">
                @{
    foreach (var item in @Model.AdContentValues)
    {
    var CheckBoxValue = _context.AdTemplateOptions.Where(e => e.AdTemplateOptionId == Convert.ToInt32(@item.ContentValue)).FirstOrDefault().OptionEn;
    <li><a ><i class="fa fa-motorcycle"></i>@CheckBoxValue</a></li>
    
    }
    }
               
            </ul>
        </div>
    </div>
</div>
<!-- list-single-main-item end -->
@*<div class="list-single-tags tags-stylwrap">
    <span class="tags-title"><i class="fas fa-tag"></i> @Model.AdTemplateConfig.AdTemplateFieldCaptionEn : </span>
    @{
    foreach (var item in @Model.AdContentValues)
    {
    var CheckBoxValue = _context.AdTemplateOptions.Where(e => e.AdTemplateOptionId == Convert.ToInt32(@item.ContentValue)).FirstOrDefault().OptionEn;

    <a> @CheckBoxValue</a>
    }
    }
    </div>*@

@*<div>
    <label for="exampleInputEmail1">@Model.AdTemplateConfig.AdTemplateFieldCaptionEn</label>
    <br />
    @{
    var LocupValue = _context.AdTemplateOptions.Where(e => e.AdTemplateOptionId == Convert.ToInt32(@Model.AdContentValues.FirstOrDefault().ContentValue)).FirstOrDefault().OptionEn;


    <p > @LocupValue</p>

    }

    </div>*@